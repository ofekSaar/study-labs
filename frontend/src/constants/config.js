// ── Quiz ──────────────────────────────────────────────────────────────────────
export const QUIZ_TIMER_SECONDS = 30;
export const SPEED_BONUS_THRESHOLD = 6; // seconds left to qualify for speed bonus

// ── XP Rewards ───────────────────────────────────────────────────────────────
export const XP_MCQ_BASE = 100;
export const XP_OPEN_BASE = 150;
export const XP_MCQ_BONUS = 200;
export const XP_OPEN_BONUS = 300;

// ── Time / Date ───────────────────────────────────────────────────────────────
export const MS_PER_DAY = 86_400_000;

// ── Course generation polling ─────────────────────────────────────────────────
export const GENERATION_POLL_INTERVAL_MS = 2000;

// ── Dashboard greeting hour thresholds ───────────────────────────────────────
export const HOUR_LATE_NIGHT_END = 5;
export const HOUR_MORNING_END = 12;
export const HOUR_AFTERNOON_END = 17;
export const HOUR_EVENING_END = 21;
